title: SymbiontAI
description: A local, offline multi-agent desktop IDE for Windows, macOS & Linux — Claude Code, Codex, Gemini, Copilot & Antigravity, in symbiosis.
theme: jekyll-theme-cayman
# The app download is a release asset (see the layout), not the repo source
# archive — so the theme's auto .zip/.tar.gz buttons are disabled.
show_downloads: false
